#find the minimum value

import numpy as np
from datetime import datetime

arr=np.random.random(100)

minN=10000

for i in arr:
  if(i<minN):
      minN=i
first=second=INTMAX
#fastMin=np.min(arr)
def minsort():
    minN=10000
    for i in range(0, leng(arr)):
        for j in range(0, len(arr[i:])):

            if arr[i] > arr[j+1]:
                arr[i]=arr[j+1]
            else:
                continue

    print(arr)
    return(arr)
#create a sort algorithm
